-- Insert subscription plans (không có user_id, vì user sẽ tham chiếu đến subscription)
-- Đây là các gói subscription template cho hệ thống

-- Xóa dữ liệu cũ nếu có
DELETE FROM subscription_feature;
DELETE FROM subscription;

-- Insert các gói subscription
INSERT INTO subscription (type, sub_name, description, price, duration_days, is_active, display_order, created_at, start_date, end_date) VALUES
('BASIC', 'BASIC_MONTHLY', N'Gói cơ bản - Phù hợp cho người dùng thường xuyên', 0.00, 30, 1, 1, GETDATE(), GETDATE(), DATEADD(YEAR, 100, GETDATE())),
('PLUS', 'PLUS_MONTHLY', N'Gói nâng cao - Nhiều ưu đãi và tiện ích hơn', 199000.00, 30, 1, 2, GETDATE(), GETDATE(), DATEADD(MONTH, 1, GETDATE())),
('PREMIUM', 'PREMIUM_YEARLY', N'Gói cao cấp - Không giới hạn, ưu đãi tối đa', 1990000.00, 365, 1, 3, GETDATE(), GETDATE(), DATEADD(YEAR, 1, GETDATE()));

-- Lưu ý:
-- - Gói BASIC là gói miễn phí mặc định cho tất cả user
-- - Các user sẽ được gán subscription_id khi đăng ký
-- - Khi user upgrade/downgrade, chỉ cần update trường subscription_id trong bảng user
